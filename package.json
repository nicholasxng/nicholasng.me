{
  "scripts": {
    "lint": "npm run lint:hugo",
    "lint:hugo": "hugo --destination /tmp/nicholasng-me-lint",
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.8.0",
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "postcss-cli": "^11.0.0"
  }
}
